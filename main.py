# Import the run_app function from the app.ui module
from app.ui import run_app

# Check if this module is being run as the main program
if __name__ == "__main__":
    # Execute the run_app function to start the application
    run_app()
